<?xml version="1.0" encoding="utf-8"?>
<navigation
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/nav_graph"
    app:startDestination="@id/nav_schedules"> <!-- Destino inicial: "Horarios" -->

    <!-- Define los fragments correspondientes a cada opción del menú -->
    <fragment
        android:id="@+id/nav_schedules"
        android:name="com.example.conectatec.EmptyFragment"
        android:label="Horarios" />

    <fragment
        android:id="@+id/nav_routes"
        android:name="com.example.conectatec.EmptyFragment"
        android:label="Rutas" />

    <fragment
        android:id="@+id/nav_history"
        android:name="com.example.conectatec.EmptyFragment"
        android:label="Historial" />

    <fragment
        android:id="@+id/nav_qr"
        android:name="com.example.conectatec.EmptyFragment"
        android:label="QR" />

</navigation>