<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Conectatec" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
         <item name="colorPrimary">@color/orange</item>
        <item name="materialButtonStyle">@style/BotonApp</item>
    </style>

    <style name="Theme.Conectatec" parent="Base.Theme.Conectatec" />
</resources>